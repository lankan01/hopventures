/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['media.licdn.com'], // Add the domain of your image source
  },
};
